---
title: Sessions
description: Sessions are agent runs inside a sandbox.
---

A **session** is an individual agent run inside a sandbox. Sessions can be seeded with a prompt or an entity (NRN).

Document in this page:
- Creating a session against a sandbox
- Seeding a session (seed prompt / seed NRN)
- Listing, stopping, and deleting sessions
