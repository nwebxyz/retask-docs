---
title: Agents
description: Configure agents that process tasks inside sandboxes.
---

An **agent** is a configured worker with a role (for example, a task processor) that runs inside a sandbox template.

Document in this page:

- Creating an agent (name, role, sandbox template)
- Available roles
- Editing and removing agents
